########################################################################
# YARA Rules Engine — profile-local override (forensic)
#
# Esta cópia sobrescreve a `conf/YaraConfig.txt` canônica APENAS para as
# chaves declaradas abaixo. Use-a para apontar o perfil `forensic` para um
# catálogo de regras dedicado (ex.: catálogo geral de IOCs/malware) sem
# afetar outros perfis.
#
# Veja conf/YaraConfig.txt (canônico) para o schema completo e defaults.
# Schema documentado em:
#   specs/001-yara-rules-engine/contracts/YaraConfig.txt.contract.md
########################################################################

# Substitua pelo caminho real do catálogo do perfil. Aceita .yar e .yara.
# Múltiplos paths separados por ";" (Windows) ou ":" (Linux).
# ruleDirectories = ${IPED_HOME}/yara-rules/forensic

# Demais chaves (maxFileSizeBytes, perItemTimeoutMs, scanAllItems,
# matchHexMaxBytes, engineLibraryHint) herdam o default do conf canônico.
